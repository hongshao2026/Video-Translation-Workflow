# 视频译配工作区强制入口

在本工作区进行任何下载、翻译、配音、重定时、渲染、验收、发布文案、章节重定位、封面生成或续跑之前，必须先完整阅读：

1. `docs/LOCAL_DUBBING_WORKFLOW.md`
2. `docs/TRANSLATION_REVIEW_SOP.md`
3. `docs/AD_DETECTION_AND_OVERLAY_SOP.md`
4. `docs/workflow.definition.json`
5. 当前视频运行目录中的 `<video_id>_run/PROJECT.md`

如当前项目另有冻结稿、术语表、音色交接或人工决定，以用户对当前项目的最新明确决定为最高优先级。不得凭旧项目默认值覆盖当前项目决定。

开始执行前，主控必须生成 `<video_id>_run/qa/workflow_lock.json`，写明上述文件的路径、版本或 SHA-256，并列出本次项目的冻结输入、当前阶段、下一道放行门以及 `media_format_selection=automatic_after_probe`、`ad_policy=detect_then_apply_evidence_based`、`translation_mode=codex_agent_direct_quality_first`、`translation_review=two_independent_agents_full_coverage`、`chapter_reading_review=required_before_translation_gate`、`chapter_reading_layout=sentence_aligned_verbatim`、`chinese_tts_speed=1.0`、`sync_strategy=video_retime_only`、`preserve_all_formal_working_master_frames=true`、`audition_authorization=voice_selection_implies_audition`、`full_tts_authorization=user_generate_full_command`、`publication_package=required_after_final_machine_qa`、`publication_text_format=utf8_txt_only`、`cover_variants=16x9_and_4x3`。没有状态为 `pass` 的工作流锁，不得开始翻译、付费 TTS、渲染或正式发布包生成。用户在音色锁定后说“生成全片”“生成全文”或等价执行口令，即视为对当前冻结翻译、角色、音色和 TTS 参数的全文付费 TTS 与渲染授权；自动 dry-run 通过后直接继续，不得再请求费用或生成确认。

## 不可违反的生产规则

- Cookie 文件路径由用户在立项时提供一次；`yt-dlp -F` 与格式探测 QA 通过后，必须按冻结的源语言、原声标记、真实分辨率、编码兼容性和码率优先级自动选择画面与音频格式，写入 `qa/media_format_selection_vN.json` 后直接下载。不得要求用户回复格式编号或组合。多音轨元数据冲突时先自动做短片段语言验证；仍无法判定则以机器门禁失败报告“待修复”，不得改成人工格式批准关卡。
- 冻结正式时间槽前必须同时检测“可整段删除的口播/赞助内容”和“需要遮盖的画面广告、二维码、促销框或角标”。检测结果、证据时间点、置信度和处置理由写入 QA；根据证据自动执行：独立口播/赞助段删除，不宜删除的画面广告定时定位遮盖，非广告内容保留。不再逐项等待用户批准。
- 广告处理后必须保留未经修改的下载母版，另建去广告/遮盖工作母版，并记录原时轴到新时轴映射。进入翻译前必须存在状态为 `pass` 的 `qa/ad_edit_gate.json`；即使未检测到广告，也要以 `no_ads_detected` 自动放行。
- 画面广告遮盖必须按检测证据验证通过的时间区间和坐标执行，不得全片误盖。渲染层级固定为“视频画面 → 广告遮盖 → 烧录字幕”；字幕必须在最上层，不能被遮盖框、角标或其他叠加层挡住。软字幕仍须保留为独立字幕轨。
- 默认采用质量优先翻译：必须由独立翻译 Agent T 直接根据冻结源文、说话人/上下文和项目术语表生成全文中文候选稿。Agent T 不得把本地模型旧译稿当作底稿，不得为适配原时间槽压缩语义，且必须保持全部稳定 ID。Codex Agent 暂不可用或额度不足时必须暂停并续跑，不得静默改用本地模型初译。
- Agent T 的候选稿冻结后，必须再由两个与 T、主控彼此不同的独立审核 Agent 同时覆盖全文：Agent A 负责中文表达与上下文，Agent B 负责语义忠实、数字、否定、主客体、扑克术语与专业逻辑。T、A、B 不得互相代替；A/B 不得只分段各审一半，也不得由主控伪装成独立审核。
- 双 Agent 审核后只能由主控合并裁决并生成新版本。裁决稿和全文回归通过后，必须按章节生成中文阅读稿并暂停给用户整体审阅；阅读稿只能按章节合并和排版裁决稿中的 `subtitle_zh`，不得改写、压缩、加入 TTS 朗读替换或提前写入未经批准的说话人结果。每个稳定 ID 必须且只能出现一次，章节时间优先使用正式工作母版时轴，并保留原视频章节时间供核对；最终发布章节仍须在视频重定时后再次映射。
- 章节阅读稿默认采用 `chapter_reading_layout=sentence_aligned_verbatim`：同一章节内连续拼接相邻稳定槽的 `subtitle_zh`，只在完整句末或章节结束处分段，避免把同一句话因字幕槽边界拆成多行；稳定 ID 必须以内联不可见标记完整、唯一、顺序保留，每槽正文必须可从阅读稿逐字重建，禁止增删可见字符、改写或跨章节移动稳定 ID。纯排版修订不必重开 T/A/B，但必须生成递增版本阅读稿、状态为 `pass` 的验证报告，并重新取得绑定阅读稿与正式翻译哈希的用户批准。
- 章节阅读稿必须落盘为 `deliverables/<video_id>_中文阅读版_按章节_vN.md`，并生成状态为 `pass` 的 `qa/chapter_reading_validation_vN.json`，记录裁决稿、章节来源、原新时轴映射、阅读稿的路径与 SHA-256，以及章节数、总槽数、缺失槽、重复槽和文本一致性结果。用户批准必须绑定阅读稿哈希和裁决稿哈希；只说“继续”不得代替该批准。
- 进入角色识别、选音或 TTS 前，必须存在状态为 `pass` 的 `qa/translation_gate.json`，其中记录翻译 Agent T 与审核 Agent A/B 的标识、模型/推理档位、全文覆盖范围、源文/术语表/候选稿哈希、三份报告或清单的路径及哈希、裁决稿哈希、术语回归结果、章节阅读稿及其验证报告的路径与哈希，以及绑定到这些哈希的用户批准状态。任何角色重复、字段缺失、哈希不匹配、阅读稿未通过或未决项不为 0，都必须停止后续阶段。
- 本地 Ollama/Qwen 只允许作为可选辅助检查器，用于结构、数字、漏译、跨槽错位或争议句第三参考；其输出没有正式翻译权，不能替代 T、A 或 B。
- 中文 TTS 必须使用模型原生 `speed=1.0`；禁止对生成后的中文音频做加速、减速、拉伸或截断有声内容。
- 对轴只能通过画面重定时解决：中文短则适当加速对应画面，中文长则适当放慢对应画面；除分析确认删除的广告区间外，必须保留正式工作母版的全部画面，原始下载母版始终不覆盖。
- 渲染前必须生成状态为 `pass` 的 `qa/production_gate.json`：确认所有 TTS 请求 `speed=1.0`、所有离线处理 `offline_rate=1.0`、中文处理链不存在 `atempo`/`rubberband`/time-stretch，视频重定时计划完整覆盖正式工作母版。没有该文件或任一检查失败，不得渲染成片。
- 字幕文本与录音文本分开管理。用户指定“只在录音时替换”的词，只能改 TTS 输入，不得改已批准字幕。
- 外文人名默认使用经源文核定的原文拉丁字母拼写，不做中文音译；该规则同时适用于字幕、章节阅读稿、发布标题/简介/章节和封面。项目若需不同显示方式，必须由用户明确决定并写入当前 `PROJECT.md` 与冻结术语表。将来的 TTS 发音提示只能写入独立录音输入，不得改变已批准的拉丁字母显示文本。
- 用户锁定角色与音色后，可直接生成对应一分钟试听，不再请求试听费用或生成批准。试听后用户发出“生成全片/全文”指令时，该指令即授权当前冻结输入的全文付费 TTS 与渲染。执行前仍必须自动 dry-run 并记录实际字符、批次、预估费用和缓存复用，但不再停下等待二次确认。
- 断点续跑必须复用已验证缓存；可能已计费但状态不确定的请求不得自动重试。
- 最终机器 QA 通过后、交付用户人工确认前，必须生成版本化发布材料：自然简体中文标题、无广告中文简介和按中文成片重定时后的章节。默认给出 3—5 个标题候选；用户明确只要一个标题时可交付一个。简介必须保留原视频链接和有内容价值的嘉宾/主题信息，删除课程推广、优惠链接、订阅引导、站点导流、无关标签和关键词堆砌。
- 面向用户的正式发布文字材料（标题候选、中文简介、重定位章节和片中书单）必须使用 UTF-8 纯文本 `.txt` 作为交付格式，不再以 `.md` 作为正式交付件；QA 状态和哈希记录仍使用 `.json`。生成版本化 TXT 后，主控还必须把同一份可见文字直接输出到聊天框，方便用户复制审核。
- 发布章节必须以最终成片 SHA-256 和实际重定时映射为输入，从“原视频 → 正式工作母版 → 中文成片”逐级换算；章节数量与来源章节一致，时间严格递增且不超过成片时长。不得把原视频时间、章节阅读稿的工作母版时间或旧成片时间直接当成最终发布章节。
- 每条正式成片默认生成两张版本化 PNG 封面：`1920×1080` 的 16:9 和 `1440×1080` 的 4:3。封面可使用用户提供且有权使用的参考图、源视频帧或重新生成的视觉；必须核对人物身份、外文人名、节目名和中文字样，不得残留广告、网址、二维码、优惠信息、订阅号召或无关标签。4:3 应重新排版关键元素，不得因机械裁切截断人物、Logo 或标题。
- 发布材料、章节映射和封面必须分别生成 `qa/publication_materials_vN.json`、`qa/cover_art_vN.json`，并由 `qa/publication_package_gate_vN.json` 绑定最终成片、最终机器 QA、章节映射、发布文案与两张封面的路径及 SHA-256。该门禁未达到 `pass` 时，不得把发布包称为可交付。
- 不得输出、记录或提交 API Key、Cookie 或临时媒体直链内容。

## 成片放行条件

正式交付前必须全部满足：

- `qa/ad_edit_gate.json`、`qa/translation_gate.json` 与 `qa/production_gate.json` 均为 `pass`，且其中记录的输入哈希与实际生产输入一致；
- 按章节中文阅读稿验证为 `pass`，全部正式字幕槽完整且唯一覆盖，用户批准记录与阅读稿及正式翻译哈希一致；
- 所有分析确认删除的广告片段已删除，所有分析确认遮盖的画面广告只在规定区间内被遮盖，字幕层位于所有遮盖层之上；
- 翻译、说话人、术语和用户指定的录音替换规则通过项目 QA；
- 中文语音为原生 1.0 速度，语音片段无重叠；
- 视频、音频、软字幕三条流存在，分辨率、帧率、帧数和时长符合计划；
- 视频包与音频包 DTS 严格递增；
- 使用 `ffmpeg -v error -xerror` 完成全片视频+音频解码且退出码为 0；
- 最终 QA 报告状态为 `pass`，并记录成片 SHA-256；
- `qa/publication_package_gate_vN.json` 为 `pass`：标题与简介无广告，最终章节按当前中文成片时轴重定位，两张封面尺寸、文字、人物和哈希均通过检查；
- 版本化交付目录中存在中文发布材料、16:9 封面和 4:3 封面，且均未覆盖旧版本；
- 机器验收通过后仍需用户完成人工抽看，才能视为最终批准。

任何一项未通过，都只能报告为“生成中”或“待修复”，不得称为已完成或已交付。

## 自动任务与额度恢复

- 用户要求定时、额度恢复后续跑或自动继续时，必须创建真实可查询的自动任务，并核对返回的任务 ID、状态和下次运行时间。
- 仅显示建议卡片或 `suggested_create` 不代表任务已经创建；没有可查询的任务记录时必须明确报告“未创建”。
- 自动任务触发后仍要遵守音色锁定、“生成全片/全文”指令授权、缓存续跑和上述成片放行条件。
