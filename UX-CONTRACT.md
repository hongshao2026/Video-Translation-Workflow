# 声轨工坊 UX 契约

本文定义新版资料库工作台的页面结构、状态含义和交互约束。它是前后端联调契约，不替代生产 SOP；发生冲突时，生产门禁优先。

## Canonical UI Map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
| --- | --- | --- | --- | --- |
| Select/Listbox | 各表单中的原生 `<select>`；弹层几何与交互由操作系统负责 | `DESIGN.md` 与本文设置/表单契约 | `native` | premium strict audit、键盘与打开状态浏览器检查 |
| Form | `app/WorkbenchShell.tsx` 的统一字段、提交状态与反馈模式 | 本文“导入视频”“设置页”“错误与恢复文案” | 导入、Provider、项目阶段操作 | `tests/rendered-html.test.mjs` 与真实后端联调 |
| Scrollbar | `app/globals.css` 的全局滚动条 token 与根级规则 | `DESIGN.md` 运行时 token 映射 | 面板可使用稳定 gutter，不得隐藏滚动条 | premium strict audit 与浏览器 computed-style 检查 |

本产品接受原生 Select/Listbox 的平台弹层外观；不得用 `appearance: none` 抹掉平台提示后继续声称为原生所有权。需要自定义弹层时，必须先建立共享、可访问的 authored Select，并同时更新本表。

## 1. 核心界面模型

### 全局顶栏

顶栏始终提供：

- 返回全部视频的品牌入口；
- 按标题、来源或语言搜索；
- 本地运行器连接状态；
- “导入视频”主操作。

连接状态只表示本地后端可访问，不代表 Provider 可用、项目门禁通过或 Worker 正在运行。

### Zotero 式三栏

桌面布局由三部分组成：

1. 左侧资料库导航：全部视频、进行中、待我处理、需修复、已完成、任务队列、运行记录与设置；
2. 中间项目列表：展示过滤后的项目、阶段、状态、更新时间和可量化进度；
3. 右侧项目检查器：展示所选项目的八阶段生产轨，以及概览、翻译、音色、QA 和历史。

小屏幕允许把左栏和右栏变为抽屉，但必须保留同一信息层级和键盘可达性，不得用移动版隐藏错误或门禁。

### 任务坞

任务坞跨项目展示后台任务。用户离开当前项目或切到设置页后，任务仍继续由本地 Worker 持有。每条任务至少显示：

- 所属项目与阶段；
- 当前状态；
- 真实进度或“不确定进度”；
- 最近一条可行动说明；
- 在状态允许时出现暂停、继续、取消或查看详情操作。

## 2. 资料库行为

### 首次加载

界面依次检查本地后端、项目列表和任务列表。状态必须明确区分：

- `loading`：正在读取，不能显示历史项目假装是真实数据；
- `live`：后端已响应且存在项目；
- `empty`：后端已响应，但资料库为空；
- `preview`：后端不可用时仅用于展示布局的演示数据。

预览数据必须带“未写入资料库”提示，不能启动任务、改变真实状态或被计入项目数量。

### 导入视频

导入对话框允许两类来源：

- `video_url`：完整视频链接；
- `local_file`：本机视频的完整路径，可选择是否复制进资料库。

保存成功后，项目进入资料库，但不会自动触发付费翻译或语音。下载、探测与运行必须由后续明确动作触发。错误保留用户输入并给出可修复说明。

前端请求必须使用后端字段：

```json
{
  "source_kind": "video_url",
  "source": "https://example.invalid/video",
  "title": "可选标题",
  "copy_local_file": false
}
```

### 搜索与筛选

搜索只过滤当前已加载项目，不改变后端。筛选计数基于真实项目；预览模式下要保持预览标识。清空搜索后恢复当前筛选，不重置选中项目。

## 3. 项目检查器

### 概览

概览展示来源、语言、媒体规格、当前运行、下一道门禁和八阶段生产轨。阶段状态只来自持久化运行数据，不能根据浏览时长或 UI 勾选推断。

“继续当前运行”需要先判断：

- 尚无运行：打开创建运行流程；
- 有可恢复任务：提交对应任务的 `resume`；
- 等待人工：打开明确的审阅/选音/批准内容；
- 配置已变化：提示创建新运行；
- 计费状态待核对：只打开请求账本，不允许直接重试。

不能把创建新运行与恢复已有运行混成同一个无条件 API 调用。

### 翻译

翻译页展示：冻结源文版本、T 候选、A/B 审核覆盖、C 裁决、术语回归、章节阅读稿和批准绑定。T/A/B/C 必须各自可追溯到 Provider 配置与报告。

批准章节阅读稿的动作必须绑定当前翻译、阅读稿和验证报告的实际 SHA-256。文件发生任何字节变化后，旧批准失效。

### 音色

音色页展示角色、Provider、模型、`voice_id`、参数和试听状态。保存并锁定音色后可以生成授权范围内的试听。正式全文生成仍要求用户发出“生成全片/全文”等明确指令，并由后端把该指令绑定到冻结输入。

界面不得提供正式中文语音的速度调节；固定展示 `1.0×`。音量、音调、情绪等能力只有 Provider 明确支持时才出现。

### QA

QA 页按门禁展示“通过 / 失败 / 待生成 / 已失效”。每项包含报告路径、输入哈希和失败原因。机器 QA 通过时文案使用“机器验收通过”，不能显示“最终批准”或“已交付”。

### 历史

历史页按时间显示运行、恢复、人工批准、Provider 请求和产物版本。敏感字段只显示“已配置 / 未配置”，不能显示 Key、Cookie、Authorization 头或完整临时下载 URL。

## 4. 八阶段状态语言

| 状态 | 用户文案 | 允许动作 |
| --- | --- | --- |
| `queued` | 排队中 | 取消；尚未发送外部请求时可暂停 |
| `running` | 运行中 | 在安全检查点请求暂停/取消 |
| `pausing` | 正在到达安全检查点 | 查看；不得重复提交 |
| `paused` | 已暂停 | 继续或取消 |
| `waiting_provider` | 等待服务 | 查看原因；用户明确继续 |
| `waiting_worker` | 另一 Worker 正在处理 | 查看租约；旧 Worker 停止后继续 |
| `repair_required` | 需修复 | 查看错误；提供修复证据后继续 |
| `blocked_uncertain` | 计费状态待核对 | 核对请求账本；禁止直接重试/取消掩盖 |
| `cancel_requested` | 等待安全取消点 | 查看 |
| `cancelled` | 已取消 | 创建新任务 |
| `completed` | 已完成此任务 | 查看产物与下一关 |

产品层可以把后端状态映射为较短标签，但不能把 `repair_required` 映射成普通“错误后自动重试”，也不能把 `blocked_uncertain` 当作可恢复错误。

## 5. 进度契约

支持三种进度表达：

- `exact`：有真实 `current`、`total`、`unit`，可显示百分比；
- `checkpointed`：按角色、批次或门禁显示离散进度；
- `indeterminate`：只显示正在做什么，不计算百分比。

规则：

- `total <= 0` 时不得渲染百分比；
- 只在后端已经验证一个语音片段或批次后增加 `current`；
- Provider 请求在网络等待中不能按经过秒数推进；
- 重连页面后以数据库状态为准，不能从零重新动画；
- 子任务 T/A/B/C 可单独显示，父任务按四个角色检查点汇总。

## 6. 设置页

设置页包含五组：

### 翻译服务

列出 LLM 配置，创建或更新时使用：

- 显示名称；
- Provider 类型；
- HTTPS 接口地址（MiniMax 可用默认地址）；
- 模型名；
- API Key；
- 非敏感 Provider 选项。

保存后 API Key 输入框立即清空；页面只显示密钥链后端、持久性和“已配置”状态。探测连接必须作为独立动作，不因保存配置自动触发生成。

### 语音服务

与翻译服务结构一致，额外展示音色目录和是否支持费用估算。正式语音设置不暴露 `speed != 1.0`。

### 工作流预设

展示预设名称、版本、锁定状态、T/A/B/C 职责、可编辑字段和 fallback 策略。内置提示词的起始指令默认锁定；只允许修改明确列出的术语、风格、项目上下文和录音替换字段。

修改预设应产生新版本或新运行，不覆盖已开始运行的提示词哈希。

### 存储与迁移

显示资料库位置、状态目录和迁移说明。当前更改资料库位置属于设备级配置，若 UI 尚未实现写入操作，按钮必须禁用并指向命令行/环境变量，不得显示保存成功。

### 高级与诊断

展示本地 API、SSE、FFmpeg、FFprobe、yt-dlp、凭证后端和 Worker 身份。路径可展示给当前本机用户，但导出诊断报告默认不得包含绝对路径或密钥值。

## 7. Provider 配置与运行冻结

创建运行的请求结构：

```json
{
  "preset_id": "quality-zh-v1",
  "role_bindings": {
    "T": "llm-primary",
    "A": "llm-review-a",
    "B": "llm-review-b",
    "C": "llm-judge"
  },
  "speech_profile_id": "speech-main"
}
```

运行创建后，检查器需要显示冻结 Provider 的厂商、模型、接口地址、能力快照和凭证是否在创建时已配置；不显示凭证引用。若用户之后改变 Provider ID、类型、模型或接口地址，当前运行执行时应拒绝，并引导创建新运行。

不提供“自动选择一个可用模型”或“失败后换下一家”的开关。fallback 固定为人工决定。

## 8. SSE 与刷新

界面通过 `GET /api/events` 建立 SSE。事件包含递增 ID、事件类型和数据库事件对象。客户端需要：

- 保存本次连接最后收到的 ID；
- 重连时通过 `Last-Event-ID` 或 `after` 恢复；
- 收到相关事件后刷新项目/任务，不假设事件载荷就是完整对象；
- 15 秒注释心跳不触发 UI 更新；
- SSE 中断时显示“事件流暂不可用”，但不把后端标为离线；
- 不以定时高频轮询替代 SSE。

当前前端如果只监听 `onmessage`，还必须补充对命名事件的统一监听或让后端同时发送默认消息事件；该项需在端到端联调中验证，不能仅凭 EventSource 已创建就视为完成。

## 9. 错误与恢复文案

错误信息由三部分组成：发生了什么、哪些结果已保存、下一步动作。例如：

> MiniMax 请求返回限流。已验证的 128 个片段仍保留；本次请求已明确失败，可在限流解除后从下一批继续。

状态不确定时必须使用更强的阻断文案：

> 服务可能已经受理请求，但本机没有收到确定结果。为避免重复计费，系统不会自动重发。请用请求标识在供应商侧核对。

禁止向用户显示原始异常中的 Key、Authorization、Cookie 或带签名 URL。未知异常显示安全错误码，并把脱敏详情保存在本机任务记录。

## 10. 可访问性与稳定布局

- 所有状态同时有文字，不只依靠颜色；
- 进度条使用正确的 `aria-valuemin/max/now`；
- 对话框打开后聚焦首个输入，Escape 可关闭，关闭后焦点返回触发按钮；
- 所有主操作使用明确动词；
- 忙碌状态保持按钮尺寸并阻止重复提交；
- `prefers-reduced-motion` 下移除位移和循环动画；
- 异步错误与进度预留空间，避免主要操作跳动；
- 表格在窄屏横向滚动，不破坏行列关系。

## 11. 当前联调清单

在宣称新版工作台可用前，至少验证：

- 项目列表与后端实际字段的映射；
- Provider 表单使用 `service_kind/provider_id/display_name/base_url/api_key/config`；
- 新建运行不发送后端不接受的字段；
- SSE 命名事件能触发刷新并能断线续接；
- 任务坞正确识别 `completed`、`repair_required` 和 `blocked_uncertain`；
- 暂停、继续、取消按钮调用对应 API，并遵守状态限制；
- 预览模式不触发任何写入或付费调用；
- 密钥没有进入浏览器存储、URL、日志和 API 响应；
- 中文源码路径下的 Vinext 已知问题有明确提示，发布验证在 ASCII 源码路径执行。
